import React from 'react';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button : {
    borderColor: "#FFFFFF",
    borderWidth: 1,
  },
  selectedButton : {
    borderColor: "#000000",
    borderWidth: 1,
  },
});
